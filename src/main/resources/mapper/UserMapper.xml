<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.phoebe.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.phoebe.entity.User">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="username" column="username"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="nickname" column="nickname"/>
        <result property="avatarUrl" column="avatar_url"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="lastLoginAt" column="last_login_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.phoebe.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, email, phone, nickname, avatar_url, status, created_at, updated_at, last_login_at)
        VALUES (#{username}, #{email}, #{phone}, #{nickname}, #{avatarUrl}, #{status}, #{createdAt}, #{updatedAt}, #{lastLoginAt})
    </insert>

    <update id="update" parameterType="com.phoebe.entity.User">
        UPDATE users
        SET username = #{username},
            email = #{email},
            phone = #{phone},
            nickname = #{nickname},
            avatar_url = #{avatarUrl},
            status = #{status},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateLastLoginAt">
        UPDATE users
        SET last_login_at = NOW(),
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <select id="findById" resultMap="UserResultMap">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <select id="findByUsername" resultMap="UserResultMap">
        SELECT * FROM users WHERE username = #{username}
    </select>

    <select id="findByEmail" resultMap="UserResultMap">
        SELECT * FROM users WHERE email = #{email}
    </select>

    <select id="findByPhone" resultMap="UserResultMap">
        SELECT * FROM users WHERE phone = #{phone}
    </select>

    <select id="findAll" resultMap="UserResultMap">
        SELECT * FROM users ORDER BY created_at DESC
    </select>

    <select id="findByStatus" resultMap="UserResultMap">
        SELECT * FROM users WHERE status = #{status} ORDER BY created_at DESC
    </select>

    <select id="searchByKeyword" resultMap="UserResultMap">
        SELECT * FROM users 
        WHERE username LIKE CONCAT('%', #{keyword}, '%')
           OR email LIKE CONCAT('%', #{keyword}, '%')
           OR nickname LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY created_at DESC
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM users
    </select>

    <select id="countByStatus" resultType="long">
        SELECT COUNT(*) FROM users WHERE status = #{status}
    </select>

    <delete id="deleteById">
        DELETE FROM users WHERE id = #{id}
    </delete>

</mapper>
